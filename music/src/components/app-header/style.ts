import styled from 'styled-components';

export const HeaderWrapper = styled.div`
    .content{
       height: 100px;
       
       display: flex;
       flex-direction: row;
       flex-wrap: nowrap;
       align-items: center;
       
       a {
           padding: 0 20px;
           white-space: nowrap;
       }
    }
    
`